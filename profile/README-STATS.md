# GitHub Stats Cards

The profile README uses repository-hosted SVG cards generated by GitHub Actions.

- `stats.svg` — GitHub statistics
- `streak.svg` — contribution streak
- `top-langs.svg` — top languages

The cards are generated by `.github/workflows/stats.yml` and committed to the `profile/` directory so the profile does not depend on a public stats API at render time.
